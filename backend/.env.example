# Comma-separated list of origins allowed to call the API (the frontend dev server).
CORS_ORIGINS=http://localhost:5173

# How long (seconds) a fetched dictionary definition stays cached before refetch.
CACHE_TTL_SECONDS=86400

# SQLite locally by default. Docker Compose can override this with Postgres.
DATABASE_URL=sqlite:///./vocabio.db
